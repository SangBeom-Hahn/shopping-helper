spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL
    username: sa
    password:
  cloud:
    gcp:
      storage:
        bucket: test
  redis:
    data:
      host: localhost
      port: 16379